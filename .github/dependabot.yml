version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
    # Dev tools and the optional server (pyproject.toml dependency groups, pinned
    # in uv.lock); Houdini's Python has no pip deps.
